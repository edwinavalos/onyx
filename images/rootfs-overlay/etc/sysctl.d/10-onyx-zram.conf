# Swapping to zram is cheaper than evicting file cache (code pages get
# re-read from the virtual disk); prefer it. page-cluster=0: no readahead
# for a RAM-backed swap device.
vm.swappiness = 100
vm.page-cluster = 0
