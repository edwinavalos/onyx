version: "2"

linters:
  default: standard
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gosec
    - misspell
    - unconvert
    - unparam
    - errorlint
    - bodyclose
    - noctx

formatters:
  enable:
    - gofmt
    - goimports
